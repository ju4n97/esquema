---
title: Introduction
description: A declarative runtime that turns HCL manifests, SQL, and Starlark into HTTP APIs.
pageType: doc-wide
---

import { OverviewGroup, SourceCode } from "@rspress/core/theme";

# Introduction

hclapi is a lightweight API engine distributed as a single binary and embeddable Go module. It turns HCL manifests into instant HTTP APIs, combining data access, validation, transformation, and route definitions in a single declarative runtime with zero-drift OpenAPI 3.1 generation.

:::info Request-time execution
Manifests are parsed and compiled once at boot time. hclapi executes pipelines dynamically per request without generating or compiling Go source.
:::

<OverviewGroup
  group={{
    name: "Getting started",
    items: [
      {
        text: "Installation",
        link: "/docs/installation",
      },
      {
        text: "Quickstart",
        link: "/docs/quickstart",
      },
      {
        text: "Why hclapi",
        link: "/docs/why",
      },
    ],
  }}
/>

<OverviewGroup
  group={{
    name: "Documentation",
    items: [
      {
        text: "Concepts",
        link: "/docs/concepts/lifecycle",
        items: [
          { text: "Request lifecycle", link: "/docs/concepts/lifecycle" },
          { text: "Execution context", link: "/docs/concepts/context" },
          { text: "Pipelines and steps", link: "/docs/concepts/pipelines" },
          {
            text: "Expressions & built-ins",
            link: "/docs/concepts/expressions",
          },
          { text: "Errors (RFC 9457)", link: "/docs/concepts/errors" },
        ],
      },
      {
        text: "Manifest",
        link: "/docs/manifest/structure",
        items: [
          { text: "Files and structure", link: "/docs/manifest/structure" },
          { text: "server", link: "/docs/manifest/server" },
          { text: "connections", link: "/docs/manifest/connections" },
          { text: "schemas", link: "/docs/manifest/schemas" },
          { text: "routes", link: "/docs/manifest/routes" },
          { text: "Scalar types", link: "/docs/manifest/types" },
          { text: "telemetry", link: "/docs/manifest/telemetry" },
        ],
      },
      {
        text: "Pipeline steps",
        link: "/docs/steps/sql",
        items: [
          { text: "sql", link: "/docs/steps/sql" },
          { text: "http", link: "/docs/steps/http" },
          { text: "valkey", link: "/docs/steps/valkey" },
          { text: "starlark", link: "/docs/steps/starlark" },
          { text: "go", link: "/docs/steps/go" },
          { text: "stream", link: "/docs/steps/stream" },
          { text: "respond", link: "/docs/steps/respond" },
          { text: "docs", link: "/docs/steps/docs" },
          { text: "spec", link: "/docs/steps/spec" },
        ],
      },
      {
        text: "Patterns",
        link: "/docs/patterns",
      },
      {
        text: "Guides",
        link: "/guides/go",
      },
      {
        text: "CLI reference",
        link: "/cli/hclapi",
      },
    ],
  }}
/>

## How hclapi works

A route consists of an HTTP method and path pattern, optional request validation, and an ordered pipeline of steps:

1. An incoming request matches against a `route` block pattern (e.g. `POST /users/{id}`).
2. Path, query, header, and body data are validated against declared schema constraints, with valid scalar parameters coerced into native Go types (`int64`, `float64`, `bool`).
3. Pipeline steps (`sql`, `http`, `valkey`, `starlark`, `go`) run sequentially in declared order and write outputs into the request context.
4. A terminal step (`respond`, `stream`, `docs`, `spec`) completes the pipeline and streams the response to the client.

See [Request lifecycle](/docs/concepts/lifecycle) for the execution model and [Pipelines and steps](/docs/concepts/pipelines) for pipeline rules.

## Source

Source code and issue tracker:

<SourceCode href="https://github.com/ju4n97/hclapi" />
